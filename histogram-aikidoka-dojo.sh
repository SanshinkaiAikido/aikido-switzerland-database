mysql --silent -h `cat connection-hostname` -u `cat connection-username` -p`cat connection-password` -D `cat connection-database` -v -e "select count(dojo.id),dojo.name from aikidoka,dojo where active = 1 and aikidoka.dojo_id = dojo.id group by dojo.id order by count(dojo.id),dojo.name"|tail -n +5
